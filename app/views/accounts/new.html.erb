<% content_for :title, "New Account - NPSP CRM" %>

<div class="max-w-4xl mx-auto">
  <div class="mb-6">
    <h1 class="text-2xl font-bold text-gray-900 dark:text-everforest-fg">New Account</h1>
    <p class="mt-1 text-sm text-gray-600 dark:text-gray-400">Add a new household or organization</p>
  </div>

  <%= render 'form', account: @account %>
</div>
