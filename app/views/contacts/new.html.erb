<% content_for :title, "New Contact - NPSP CRM" %>

<div class="max-w-4xl mx-auto">
  <div class="mb-6">
    <h1 class="text-2xl font-bold text-gray-900 dark:text-everforest-fg">New Contact</h1>
    <p class="mt-1 text-sm text-gray-600 dark:text-gray-400">Add a new contact to your database</p>
  </div>

  <%= render 'form', contact: @contact %>
</div>
