<% content_for :title, "New Recurring Donation - NPSP CRM" %>

<div class="max-w-4xl mx-auto">
  <div class="mb-6">
    <h1 class="text-2xl font-bold text-gray-900 dark:text-everforest-fg">New Recurring Donation</h1>
    <p class="mt-1 text-sm text-gray-600 dark:text-gray-400">Set up a new sustainer or recurring gift</p>
  </div>

  <%= render 'form', recurring_donation: @recurring_donation %>
</div>
